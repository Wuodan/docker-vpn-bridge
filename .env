TARGET_IP=10.7.2.100
TARGET_PORT=8080

LOCAL_PORT=18080
# set to Docker bridge IP (docker0)
# find it with:
# ip -4 addr show docker0 | awk '/inet /{print $2}' | cut -d/ -f1
# typically 172.17.0.1
DOCKER_BRIDGE_IP=172.17.0.1

